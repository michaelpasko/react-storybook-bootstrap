const debug = (message) => {
    console.log(message)
}

const info = (message) => {
    console.info(message)
}

const error = (message) => {
    console.error(message)
}

export default {
    debug,
    info,
    error
}